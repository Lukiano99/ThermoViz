import { RegisterView } from "src/sections/auth";

// ----------------------------------------------------------------------

export const metadata = {
  title: "Registration",
};

export default function RegisterPage() {
  return <RegisterView />;
}
